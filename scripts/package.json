{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "generate-map": "esbuild src/generate-project-map.ts --bundle --platform=node --format=cjs --external:pdfkit --outfile=gen-map.cjs && node gen-map.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pdfkit": "^0.17.6",
    "esbuild": "catalog:",
    "pdfkit": "^0.19.1",
    "tsx": "catalog:"
  }
}
